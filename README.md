# shopcarts

To run the flask app - 
    1) ``` vagrant up ```
    2) ``` vagrant ssh ``` 
    3) ``` cd /vagrant ``` 
    4) ``` export FLASK_APP= services/__init__.py ```
    5) ``` flask run --host=0.0.0.0 ``` 